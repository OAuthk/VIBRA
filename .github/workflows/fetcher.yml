# .github/workflows/fetcher.yml
name: Fetch and Cache Trend Data

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'scripts/**'
      - 'models.py'
      - 'requirements.txt'
      - '.github/workflows/fetcher.yml'

jobs:
  fetch-and-cache:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch for scripts
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install Python dependencies
        run: pip install -r requirements.txt

      # ★★★★★★★★★★
      # ★ 変更点 ★
      # ★★★★★★★★★★
      - name: 🎭 Install Playwright Browsers to a predictable path
        run: playwright install --with-deps chromium
        env:
          # ★ ブラウザのインストール先を、プロジェクトのディレクトリ内の 'pw-browsers' に強制する
          PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/pw-browsers

      - name: 📥 Restore scores cache
        id: scores-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: cache/previous_scores.json
          key: scores-cache-v1

      - name: 🐍 Run Data Pipeline
        env:
          MERCARI_AFFILIATE_ID: ${{ secrets.MERCARI_AFFILIATE_ID }}
          GA4_TRACKING_ID: ${{ secrets.GA4_TRACKING_ID }}
          # ★ Pythonスクリプト実行時にも、同じカスタムパスを教える
          PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/pw-browsers
        run: python scripts/main.py

      # ... (以降のキャッシュ保存とGitプッシュのステップは変更なし)
      - name: 💾 Save current scores to cache for next run
        # ...
      - name: 🚀 Commit and Push data to cache-branch
        # ...